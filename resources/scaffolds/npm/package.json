{
  "name": "{{name}}",
  "version": "0.1.0",
  "private": true,
  "description": "studyAgent 实战工坊 demo（零依赖，可离线构建/启动/测试）",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node scripts/serve.js",
    "test": "node scripts/test.js"
  }
}
